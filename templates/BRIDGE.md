# BRIDGE — <project name>

<!-- Stable handoff context: what any agent or person needs to pick up this
     project cold. Update only when a decision or constraint changes.
     Day-to-day movement belongs in STATUS.md. Keep under one screen. -->

## What this project is

<Two to five sentences: what it does, for whom, and what "done" looks like.>

## Decisions made (and why)

1. <Decision> — <the reason, one line. The reason is what prevents the
   decision from being relitigated.>
2. <Decision> — <reason>

## Key files and entry points

- `<path>` — <what it is / why it matters>
- `<path>` — <what it is>

## Constraints

- <Hard rule: things that must never be done, deadlines, external
  dependencies, environments that share state.>

## How to verify it works

<The one command or manual check that proves the project is healthy.>
